/*
 ============================================================================
 Progettare un programma che abbia una classe che descriva gli elementi di arredo con i seguenti attributi:
 classe arredo: materiale, collocazione mobili, colore, prezzo al rivenditore , prezzo al cliente, sconto
 Il metodo prevede il calcolo del prezzo di vendita effettivo, ricevendo il parametro della sconto.
 -Se 80<100 il compilatroe deve dare 1 warning: "Lo sconto e' troppo alto";
 classe credenza: n ripiani, n cassetti
 Se la credenza ha inolte n cassetti, lo sconto massimo e' del 20%
 classe letto: letto singolo, matrimonilae, a castello
 Il programma legge dal catalogo del rivenditore(letti,credenze...) e seleziona uno di questi oggetti , dando in output inforamzioni e prezzo

 ============================================================================
 */
#include <iostream>
using namespace std;


/*arredo                 letto                 credenza
 materiale            tipo
 colore                    +                    +
 prezzo rivenditore
 prezzo al listino
 prezzo di vendita

 calcola vendita(int)
 +arredi        -arredi
 catalogo.txt
 Arredi: cc    cc    cc    100    200    000
 Arrad    dd    dd    dd    120    180    000



 */
p rat{
    char materiale[20];
    char colore;
    float prezzo_riven;    //rivenditore
    float prezzo_list;     //listino
    float sconto;
    arredo(char[100]);
}
public class letto % public arredo
protected char tipo_letto;

//"r","m"-> matrimoniale
//"s","s"->singolo
//"c" "c" ->castello
" "

int main() {
    class arredo
    cout ("inserisci n\n");
    fp=fopen("catalogo.txt","r");
    float pb,pl,pv,n_r,n_c;
    class tipo_letto
    while{feof(fp)){
        fscanf(fp,classe,sizeof(classe))
        if(stcmp(classe,"feval")==0)
            fscanf(fp,materiali,sizeof(materiale))
            c1[a]=//....
            }
        //........


        return 0;
    }
    double virtual calcolo_vendita(int sconto){
        int p.v=prezzo_list; //e' uguale allo sconto /100
        if(p.v<prezzo_riven)        return 1;
        else retun p.v;



    }

